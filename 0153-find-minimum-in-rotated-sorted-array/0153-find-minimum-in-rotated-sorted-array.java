class Solution {
    public int findMin(int[] arr) {
        int lo=0;int hi=arr.length-1;
        Arrays.sort(arr);
        return arr[0];
      //  while(lo<=hi){
    //        int mid = (lo+hi)/2;
  //          if(arr[mid]>=arr[lo]){
//                if()
          //  }
      //  }
    }
}